wiki/.index/
# Multitenant runtime data (per-tenant stores + the tenant registry) created by a
# local run under the default data root; never committed to the project repo.
wiki/tenants/
wiki/registry.json
wiki/credentials.json
wiki/system_audit.jsonl
wiki/auth_throttle.json
wiki/reset_tokens.json
wiki/auth_audit.jsonl
.venv
__pycache__/
*.pyc
.env
*.egg-info/
build/
dist/
notes_inbox/*
!notes_inbox/.gitkeep
action_outbox/*
!action_outbox/.gitkeep
mnesis_agents_runs/
mnesis_agents.checkpoints.db*

# Local demo/design artifacts — kept out of git to keep the repo lean (screen
# recordings, voiceovers, logo bundle, the architecture doc).
*.mov
*.m4a
*.docx
logos.zip
