# Build-time default for the API base URL (overridable at runtime via public/config.js).
VITE_API_BASE=/api

# Optional build-time token fallback. Prefer setting the token at RUNTIME in
# public/config.js (window.__MNESIS_CONFIG__.token) so the bundle stays generic.
VITE_API_TOKEN=
