# Open Knowledge Format (OKF) v0.1 — Mnesis conformance reference

Mnesis adopts **Google Cloud's Open Knowledge Format v0.1** as the interchange contract
for its canonical Markdown entries. This is a short, in-repo reference **grounded in the
spec** ([`GoogleCloudPlatform/knowledge-catalog/okf/SPEC.md`](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md)),
plus the Mnesis↔OKF reconciliation. The contract + validator live in
[`src/mnesis/okf.py`](../src/mnesis/okf.py); the canonical schema is stated in
[`CLAUDE.md` §4](../CLAUDE.md).

> **Status:** OKF1 defined the *contract + validator*; **OKF2 made the store write path
> OKF-conformant** — every page is written as an OKF concept document (validated before
> commit, fail-closed via `store.OKFConformanceError`), the body carries generated OKF
> cross-links, and the reserved files `index.md`/`log.md` are generated per write.
> **OKF3 migrates existing corpora** with `mnesis migrate-okf` — a lossless, idempotent,
> reversible, per-tenant re-serialization (git-tag backup + `--dry-run`/`--rollback`) that
> preserves every field/source/relation/supersession link and never refreshes timestamps.
> **OKF4 rebuilds the caches from OKF entries** — the FTS index also indexes the concept
> `type`, and the graph folds in OKF cross-links (as `related_to`) reconciled with the
> richer typed `relations`; a migrated corpus reindexes/rebuilds to equivalent results.

## 1. The OKF v0.1 contract (as specified)

**Required frontmatter**

| Field | Type | Meaning |
|---|---|---|
| `type` | string (non-empty) | A short string naming the kind of concept. Not centrally registered; producers pick self-explanatory values; **consumers must tolerate unknown values**. |

**Recommended frontmatter** (all optional — a consumer MUST NOT reject on absence)

| Field | Type | Meaning |
|---|---|---|
| `title` | string | Human display name; a consumer may derive it from the filename if omitted. |
| `description` | string | One-sentence summary for indexes/previews. |
| `resource` | URI | Identifies the underlying asset; **omitted for abstract concepts**. |
| `tags` | list[string] | Cross-cutting categorization. |
| `timestamp` | ISO 8601 | Datetime of the last meaningful change. |

Producers **MAY add arbitrary additional fields**; consumers **SHOULD preserve unknown
keys** when round-tripping. The OKF **reference parser** reads `type` / `title` /
`description` / `timestamp` — Mnesis targets that set for interop robustness.

**Conformance criteria (a bundle conforms iff):**
1. Every non-reserved `.md` file contains *parseable* YAML frontmatter.
2. Every frontmatter block contains a **non-empty `type`**.
3. Reserved filenames follow their specified structures when present.

**Consumers MUST NOT reject a bundle for:** missing optional fields · unknown `type`
values · unknown extra keys · broken cross-links · missing `index.md`.

**Identity.** A concept's id is its **bundle-relative path with `.md` removed**
(`tables/users.md` → `tables/users`). Directory structure is producer-defined.

**Cross-links.** Ordinary Markdown links. **Bundle-absolute** links (begin with `/`,
relative to the bundle root) are *recommended* — they stay stable when files move;
relative links are also allowed. A link from A to B asserts a **relationship**; the
*kind* is conveyed by the **surrounding prose**, not by the link. Consumers **MUST
tolerate broken links**.

**Reserved files.**

| File | Structure |
|---|---|
| `index.md` | A directory listing for progressive disclosure — **no frontmatter permitted**. |
| `log.md` | Update history — **ISO 8601 date headings** with prose entries. |

**Lenient vs. strict.** *Strict* (validator **errors**): frontmatter must parse; `type`
present + non-empty; reserved-file formats. *Lenient* (validator **warnings** or ignored):
unknown `type`, extra keys, broken links, missing optional fields, unknown headings —
"OKF is meant to remain useful as bundles grow, get refactored, and are partially
generated by agents."

## 2. The Mnesis bundle

The OKF **bundle** for a tenant is its **`pages/`** directory — the canonical concept
files, one `.md` per page (`sources/` is redacted provenance, not concepts; `.cache/` is
rebuildable). So a page at `pages/<id>.md` has OKF concept id **`<id>`**. Each tenant is
its own bundle (its own git repo), matching OKF's "git repository (recommended)"
distribution. `index.md` / `log.md` are reserved at the bundle root.

## 3. Field mapping (Mnesis ↔ OKF)

Mnesis is a **strict superset** of the OKF core: it fills the OKF-core fields and carries
the rest as tolerated **extension** keys (unknown keys OKF consumers preserve and ignore).
`okf.to_okf_metadata(page)` implements this.

| OKF core field | Source in Mnesis | Notes |
|---|---|---|
| `type` *(required)* | `kind` (`fact` \| `digest` \| `note`) | The concept type mirrors Mnesis `kind`; `kind` is **also** retained as an extension for existing code. |
| `title` | `title` | 1:1. |
| `description` | first sentence of the page body | Derived (Mnesis has no `description` field); falls back to `title`. |
| `timestamp` | `updated` | ISO 8601 (µs + `Z`) — the last meaningful change. |
| `tags` | `tags` | 1:1 (`type:value` + free tags). |
| `resource` | *(omitted)* | Mnesis pages are abstract concepts; provenance lives in `sources` (an extension), not an external asset URI. |

| Mnesis **extension** field (OKF-tolerated) | Purpose |
|---|---|
| `id` | Alias of the path-derived concept identity (convenience; the **path** is canonical). |
| `kind` · `status` · `decay_class` | Page kind / lifecycle state / decay override. |
| `created` · `last_confirmed` · `source_count` · `sources` | Provenance + confidence inputs. |
| `supersedes` · `superseded_by` · `contradicts` · `relations` | Lifecycle + typed knowledge-graph edges. |
| `owner_principal` · `visibility` | Tenancy ownership + within-tenant visibility (IAM/§16). |
| `question` | Digest-only: the question a filed-back answer resolved. |

**Identity & cross-links in Mnesis terms.** The **path** (`pages/<id>.md` → `<id>`) is the
canonical concept id; the `id` frontmatter key is a retained alias that equals it.
Mnesis's typed `relations` edges and `[[page-id]]` body citations are *extensions* /
alternate encodings of relationships; the OKF-native convention is a **Markdown link to
`/<concept-id>`** (bundle-absolute), with the relationship kind in the prose.

## 4. The validator

`okf.validate_document(text, path=…)` and `okf.validate_bundle(root)` return an
`OKFReport` (`conformant` = no error-level issues; warnings never break conformance).
They enforce the strict criteria as errors and surface the reference-parser gaps as
warnings, honoring OKF's leniency (broken links, unknown keys/types, missing optional
fields are **not** errors). See `tests/test_okf_validator.py`.

## 5. Migrating existing corpora (OKF3)

`mnesis migrate-okf` converts a tenant's existing `pages/` bundle to OKF form by
re-reading each page (the reader tolerates **both** the pre- and post-OKF shapes) and
re-serializing it through the §3 mapping. Guarantees:

- **Lossless.** Every field, `sources` link, `relations` triple, and supersession link
  (`supersedes`/`superseded_by`/`contradicts`, and `status`) is preserved verbatim; the
  page round-trips to an identical `store.Page`. References stay resolvable.
- **No semantic change.** Timestamps are **not** refreshed — `updated`/`timestamp`,
  `created`, and `last_confirmed` are carried over unchanged, so confidence, decay, and
  supersession *behaviour* is identical; only the on-disk representation changes.
- **Validated.** Every migrated entry passes `okf.validate_document` before it is written
  (fail-closed).
- **Idempotent.** A page already byte-identical to its OKF target is left untouched;
  a re-run with nothing to convert makes no commit (`--dry-run` reports without writing).
- **Reversible + per-tenant.** The pre-migration HEAD is tagged `pre-okf-migration`
  (backup); `mnesis migrate-okf --rollback` `git reset --hard`s to it, restoring the
  original byte-for-byte. Each tenant is its own git bundle, so a migration only ever
  touches the bound tenant. See `tests/test_okf_migration.py`.
